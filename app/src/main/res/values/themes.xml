<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Transparent_Wallpaper" parent="Theme.Material3.Light.NoActionBar">
        <item name="dialogTheme">@android:color/transparent</item>
        <item name="alertDialogTheme">@android:color/transparent</item>
        <item name="alertDialogStyle">@android:color/transparent</item>
    </style>

    <style name="Theme.Transparent_Wallpaper" parent="Base.Theme.Transparent_Wallpaper" />

    <style name="BaseDialog" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:windowBackground">@android:color/black</item>
    </style>
</resources>